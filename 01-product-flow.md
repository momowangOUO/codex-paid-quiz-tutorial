# 01. 产品流程怎么拆

**【本章核心】写代码前先把“用户会看到哪些页面、每一步做什么、哪一步需要后端”拆清楚。**

开始写代码前，你只需要先回答一个问题：

```text
用户从进站到付费解锁，会经过哪些画面？
```

这一章不讲架构图，先把产品拆成 Codex 能理解的小任务。

> 💡 **为什么这样做？**
> Codex 需要明确的任务边界。产品流程像一张路线图：先知道用户从哪里进来、在哪里答题、在哪里付款、在哪里看报告，后面写代码才不会东补一块、西补一块。

## 先画最短用户路径

**【本节核心】先写出用户从首页到完整报告的最短路线。**

付费测验网站的最短路径是：

```text
首页
  -> 答题页
  -> 免费导言报告
  -> 付款页
  -> 完整报告
  -> 保存或再次打开报告
```

你可以先不要做登录。低价一次性报告最重要的是减少阻力。

> 💡 **为什么这样做？**
> 登录、会员、后台、优惠码都可以以后再加。第一版先让用户能完成一次完整购买，就像先确认柜台真的能收钱，再装修更多货架。

## 每个页面要做什么

**【本节核心】每个页面只负责一个主要任务，避免第一版变得过重。**

### 首页

首页只负责让用户开始。

需要有：

- 一句话说明这个测验帮用户解决什么问题。
- 开始按钮。
- 可选：语言切换、案例入口。

不要一开始就写“付费解锁”“马上购买”。用户还没投入时间，太早提钱会降低完成率。

### 答题页

答题页只负责让用户完成题目。

需要有：

- 当前题号。
- 题目。
- 选项。
- 上一题/下一题。
- 进度提示。
- 图片或视觉辅助。

新手建议先做 5 题测试版，流程跑通后再扩成 48 题。

### 免费导言报告

免费版不是故意吊胃口，也不是空话。它要让用户觉得你专业。

可以显示：

- 核心心理驱动力。
- 行为倾向与潜在卡点。
- 自我认知与外在表现差距。
- 完整报告会继续分析哪些方向。

不要显示：

- 完整职业答案。
- 完整路径。
- 详细行动计划。
- 可保存的完整报告。

### 付费墙

付费墙只负责让用户付款。

需要有：

- 当前价格。
- 一个清楚的付款按钮。
- 付款后自动解锁说明。
- 付款失败或等待中的状态。

不要同时塞太多付款方式按钮。后端可以根据地区推荐一个入口。

### 完整报告

完整报告要让用户觉得“这不是随便生成一段文字”。

可以包含：

- 职业雷达。
- 心理侧写。
- 适合的职业类型。
- 多条发展路线。
- 风险边界。
- 备选方向。
- 保存报告。

## 用表格交给 Codex

**【本节核心】把页面、用户动作、后端参与方式整理成表格，再交给 Codex。**

把你的产品流程整理成这张表，再贴给 Codex：

| 页面 | 用户看到什么 | 用户能做什么 | 后端要不要参与 |
| --- | --- | --- | --- |
| 首页 | 产品说明、开始按钮 | 开始测验 | 不需要 |
| 答题页 | 题目和选项 | 作答、下一题 | 不需要，除非要实时保存 |
| 免费报告 | 部分分析、解锁按钮 | 点击付款 | 需要创建 payment session |
| Paddle checkout | 付款表单 | 付款 | Paddle 处理 |
| 完整报告 | 全部分析 | 查看、保存 | 需要验证 access token |

## reportId 是什么

**【本节核心】`reportId` 是某一份测验报告的编号。**

`reportId` 是一份报告的编号。

用户答完题后，前端生成或后端生成一个 `reportId`，后续所有动作都围绕它：

```text
这份答案 -> reportId
这笔订单 -> reportId
这次解锁 -> reportId
这份完整报告 -> reportId
```

没有 `reportId`，付款平台告诉你“有人付钱了”，你也不知道该解锁哪份报告。

> 💡 **为什么这样做？**
> `reportId` 像取餐号码。用户点了餐、付了钱、回来取餐，都要靠同一个号码对应到同一份内容。

## payment session 是什么

**【本节核心】`payment_session` 是某一次付款尝试的记录。**

`payment_session` 是一次付款尝试。

一个用户可能：

- 点击付款但关闭页面。
- 付款失败。
- 重试付款。
- 付款成功但前端没跳回来。

所以不要只靠前端状态。后端要保存：

```json
{
  "sessionId": "pay_xxx",
  "reportId": "rep_xxx",
  "provider": "paddle",
  "providerOrderId": "txn_xxx",
  "status": "pending"
}
```

付款成功后改成：

```json
{
  "status": "paid",
  "paidAt": "2026-05-28T00:00:00.000Z"
}
```

> 💡 **为什么这样做？**
> 一份报告可能会有多次付款尝试。用户可能第一次关掉付款页，第二次才成功。`payment_session` 可以把这些尝试分开记录，避免状态混乱。

## access token 是什么

**【本节核心】`accessToken` 是付款成功后打开完整报告的钥匙。**

`accessToken` 是完整报告的钥匙。

付款成功后，后端给这份报告发一个不可猜的 token。用户下次回来时，前端带 token 问后端：

```text
这个 token 有没有权限看 reportId 这份报告？
```

后端说可以，才显示完整报告。

> 💡 **为什么这样做？**
> 不应该只靠“这个 reportId 已经有人付过钱”来显示完整报告，否则别人刷到同一个 reportId 可能也能看。`accessToken` 像专属钥匙，只有拿到钥匙的人才能打开。

## 第一版可以先不做什么

**【本节核心】第一版只保留一次性付费解锁，先不做会拖慢上线的复杂功能。**

为了更快上线，第一版可以先不做：

- 用户注册。
- 后台 CMS。
- 优惠码。
- 订阅。
- 复杂数据库。
- 自动邮件。
- 多个产品套餐。

你先跑通一次性付款解锁，再慢慢加。

> 💡 **为什么这样做？**
> 产品早期最需要验证的是“有没有人愿意完成测验并付款”。先把这条主线跑通，比一开始做很多周边功能更有价值。

## 给 Codex 的产品流程提示词

**【本节核心】把已经拆好的产品流程直接变成 Codex 可以执行的提示词。**

复制这段：

```text
我想做一个一次性付费解锁报告的测验网站。

请按照这个用户路径设计代码：
首页 -> 答题页 -> 免费导言报告 -> 付款 -> 完整报告

第一版不要登录、不要订阅、不要优惠码。

请帮我设计：
1. 前端页面状态
2. reportId 如何生成和保存
3. payment session 数据结构
4. access token 解锁逻辑
5. mock 支付测试流程

请先输出文件计划，再实现最小版本。
```

## 本章完成标准

**【本节核心】完成本章后，你应该能用普通话解释清楚整个付费解锁流程。**

完成后你应该能清楚说出：

- 用户会经过哪些页面。
- 哪一步需要后端。
- 为什么不能只靠前端判断已付款。
- `reportId`、`payment_session`、`accessToken` 各自负责什么。

下一章会开始让 Codex 真正写代码。
